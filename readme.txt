This plugin is a simple template for authenticating users against a external user repository.

The basic idea is that you can implement the lib.loginManager.lookupUser() method against you custom repository.